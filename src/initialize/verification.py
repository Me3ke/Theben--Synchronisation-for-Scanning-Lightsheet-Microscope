class Verification:
    def __init__(self, mode, sequence, setup, param):
        self.mode = mode
        self.sequence = sequence
        self.setup = setup
        self.param = param

    def verify(self):
        pass
